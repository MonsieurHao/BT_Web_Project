{% extends "layout.html.twig" %}
{% block bundle_body %}
    Thomas' Page

    {%  for article in articles %}
        <div class="article" >
            This article were posted on {{ article.date}} <br>
            By {{ article.title }} : <br>
            {{ article.content }}
            <br>
            <br>
            <a href="{{ path('bt_blog_viewArticle',{'id':article.id}) }}"> More...</a>
        </div>
    {% else %} <div class="article">No article</div>
    {% endfor %}

{% endblock %}